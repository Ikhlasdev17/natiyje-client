import { withLayout } from '@/layouts/layout'
import { ProfilePageComponent } from '@/page-components'

const Profile = () => {
	return (
		<>
			<ProfilePageComponent />
		</>
	)
}

export default withLayout(Profile)
